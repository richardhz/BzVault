@inherits DisplayItemListBase

<MudList Clickable="true">
    @foreach (var rec in Records)
    {
        <MudListItem Text="@rec.Name" OnClick="@(() => DoDetail(rec.Id))"></MudListItem>
    }
</MudList>







